uri: https://github.com/berquerant/openai-chat-web
branch: main
locald: repos/openai-chat-web
lock: locks/openai-chat-web.lock
install:
  - pipenv --rm | echo "First time install"
  - pipenv install
uninstall:
  - pipenv --rm
